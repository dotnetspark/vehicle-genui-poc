# Ash Test Report — chore/tests/demo-a-b

**Date:** 2026-05-XX  
**Agent:** Ash (Tester)  
**PR:** https://github.com/dotnetspark/vehicle-genui-poc/pull/44  
**Branch:** `chore/tests/demo-a-b`  
**Tested PRs:** `feat/demo-a/content-hash-uri`, `feat/demo-b/frontend-improvements`

---

## Verdict: ✓ PASS — Both suites green, no blocking issues

---

## Test Results Summary

| Suite | Runner | Tests | Pass | Fail |
|---|---|---|---|---|
| Demo A — query-cache | node:test | 20 | 20 | 0 |
| Demo A — resource-uri | node:test | 12 | 12 | 0 |
| Demo B — frontend (Vitest) | vitest run | 63 | 63 | 0 |
| **Total** | | **95** | **95** | **0** |

---

## Demo A Findings

### query-cache (20 tests)
- Original 11 singleton tests still pass — no regressions from adding `createQueryCache` factory.
- Key normalisation: whitespace variants and case variants hash to *different* keys (raw string identity, by design). Tests document this behaviour explicitly.
- LRU eviction at `max=3`: least-recently-used entry is correctly displaced; `get()` promotes to MRU; `size` never exceeds `max`.

### resource-uri (12 tests)
- `resolveResourceUri` correctly prefers the `uri` field from `dist/resource-uri.json` over runtime hash computation.
- Runtime hash path produces deterministic 16-char lowercase hex SHA-256 truncation.
- Fallback `FALLBACK_URI` (`ui://vehicle/chart-renderer/mcp-app.v4.html`) is returned only when both manifest and HTML are absent.
- Integration test reads the committed `dist/resource-uri.json` and confirms the live URI matches.

### Notes on content-hash-uri PR
The PR changes the manifest field from `uri` → `resourceUri` and shortens the hash to 12 chars. Tests in this branch target the **main** format (`uri`, 16-char). Once that PR merges, `resource-uri.ts` and its tests will need updating (field name + hash length).

---

## Demo B Findings

### Components
- `PanelErrorBoundary` — error boundary catches child throws, displays Retry + optional raw-data pane. Retry recovery works (controlled-throw pattern in tests).
- `ProgressPanel` — renders correct badge text for all three states (`pending`, `inProgress`, `executing`).
- `Dashboard` — empty state, all panel kinds (skeleton/fuel/makes/trend/error), multi-panel, PanelErrorBoundary wrapping all verified.

### State
- `usePanels` — module-level store correctly notifies subscribers; `clearAll` resets; `error` kind round-trips correctly.

### Schemas (Zod)
- All three `ZShow*Args` schemas validate correct inputs and reject: empty `panelId`, negative counts, `percentage > 100`, empty arrays, `series.length > 4`.

### Warnings (non-blocking)
- Dashboard and usePanels tests emit React `act()` warnings. These are caused by CopilotKit's internal subscription firing during initial render (before any user interaction). All assertions pass; this is a cosmetic issue in the test output, not a bug in the production code.

---

## Recommendations

1. **Merge order:** `feat/demo-a/content-hash-uri` and `feat/demo-b/frontend-improvements` should be merged before or alongside this PR. After `feat/demo-a/content-hash-uri` merges, update `resource-uri.ts` field name (`uri` → `resourceUri`) and hash length (16 → 12 chars), then update the corresponding tests.
2. **act() warnings:** Consider wrapping `Dashboard` render in `act()` from `@testing-library/react` if the warnings become noise. Not blocking.
3. **CI integration:** `scripts/run-tests.sh` is ready for a GitHub Actions workflow step. Add a job that runs it against Node 22 + pnpm 9.
